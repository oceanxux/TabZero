name: Chrome Extension 

on:
  push:
    tags:
      - 'v*.*.*'   # 仅当推送 v1.0.0 这样格式的标签时触发

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # 创建 Release 需要这个权限

    steps:
      - name: 1. Checkout code
        uses: actions/checkout@v4

      - name: 2. Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 3. Install dependencies
        run: npm install --legacy-peer-deps

      - name: 4. Build extension
        run: npm run build

      - name: 5. Archive zip with version
        run: zip -r tabzero-${{ github.ref_name }}.zip TabZero/

      - name: 6. Create GitHub Release and attach ZIP
        uses: softprops/action-gh-release@v1
        with:
          files: tabzero-${{ github.ref_name }}.zip
          name: Release ${{ github.ref_name }}
          body: |
            🎉 **新版本发布：${{ github.ref_name }}**

            此版本包含以下构建产物：
            - `tabzero-${{ github.ref_name }}.zip` (Chrome 扩展文件)
